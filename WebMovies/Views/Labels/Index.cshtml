@model IEnumerable<WebMovies.Models.Label>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.labelText)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.labelText)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.labelId }) |
                @Html.ActionLink("Details", "Details", new { id=item.labelId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.labelId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
